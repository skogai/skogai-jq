{
  "name": "crud-get",
  "description": "Get value at a nested path",
  "examples": [
    {
      "description": "Get existing value",
      "input": { "user": { "name": "alice" } },
      "args": { "path": "user.name" },
      "output": "alice"
    }
  ]
}
